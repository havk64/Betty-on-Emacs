#include <stdio.h>

int main()
{
	/* To see the list of errors, press `Ctrl-c ! l` */
	/* To see the list of checkers, press `Ctrl-c ! v` */

	printf("");

	/* Note the low CPU utilization: `$ top -o %CPU` */

	return 0;
}
